apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml --chart
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: logger-service
  name: logger-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: logger-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml --chart
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: logger-service
    spec:
      containers:
        - image: logger-service
          name: logger-service
      restartPolicy: Always
